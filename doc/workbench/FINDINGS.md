# Workbench findings

Bugs and oddities the Recoil Workbench has surfaced, newest first. Each entry says how it was found and what happened to it.

| Date | Area | Finding | How found | Status |
|---|---|---|---|---|
| 2026-09-24 | Workbench (test design) | `corsiegebreaker` never fired (diagnostic: "dreadovercharge idle, firestate 2"): its weapon costs 1500 energy per shot and the arena team had none. `armgremlin` never fired either: BAR's cloak widget puts cloaked units of the local player on hold fire. | `weapon_range_all` "which weapons fired" diagnostic | Fixed: per-unit scenarios get unlimited resources (`arena.unlimitedResources`) and attackers are decloaked; both pass |
| 2026-09-24 | Coverage | New `air_attack`: all 18 armed aircraft that can hurt a structure strike one 1500 elmos away (first hit 5.0-13.3 s). New engine unit test `test_KeyInput` guards the low-fps drag-build modifier fix (mutation: reverting the fix fails 4 of 6 assertions). | `air_attack`, `test_KeyInput` | Baseline recorded |
| 2026-09-24 | Workbench (test design) | `weapon_range_all` "outside" failures were shells from the previous batch still in flight after their shooters were removed, landing on the next batch's targets (found once hits were attributed: "other ?/armcroc_arm_triton 225" on `corstorm`'s target). | `UnitDamaged` attribution via the new `syncedCallins` | Fixed: `arena.clear` deletes every projectile |
| 2026-09-24 | Workbench (test design) | Range model: the spies' only weapon is a zero-damage dummy (`bogus`), the evolving commanders' longest weapon is a sea laser whose script only aims underwater, and stockpiled missiles, splash and flames reach past the nominal range. | `weapon_range_all` failures, weapon defs and unit scripts reviewed | Fixed: "must hit" and "can reach" ranges (see the scenario header) |
| 2026-09-24 | Coverage | New `ship_movement`: all 52 ship and submarine types cross a flooded arena within budget and speed limits, 104/104. | `ship_movement` | Baseline recorded |
| 2026-09-24 | Coverage | New `unit_behaviours`: all 23 land factories produce their cheapest unit, all 68 mobile builders finish their cheapest structure, an Atlas carries a Pawn to within 23 elmos of the drop point, a spy cloaks and stays cloaked, a radar shows an enemy that is outside LOS. 94/94 pass. | `unit_behaviours` | Baseline recorded |
| 2026-09-24 | Workbench | A harness change (per-instance call ids) read `os.clock()` at file scope; the harness is also loaded by synced code, where `os` is nil, so the synced gadget failed to load and every `ctx.call` timed out. | Smoke suite (api_selftest `synced_call_roundtrip` and the infolog widget-load scan) on the first run after the change | Fixed (ebadad6ee8) |
| 2026-09-24 | Workbench (test design) | `weapon_range_all` placed lanes at x = 15% of the map over real terrain with the start commanders active: 39 of 248 cases failed, many of them units that passed the same check at the map centre (`armrock`). | `weapon_range_all` | Fixed: per-unit scenarios now run on a levelled, featureless arena with pre-existing units holding fire (`workbench/lib/arena.lua`); rerun pending |
| 2026-09-24 | BAR / engine movement | `armsptk` (all-terrain spider) reached 57.5 elmos/s against its maxSpeed 47 (22% over) crossing the map centre; the other 172 mobile ground unit types stayed within 10% and all arrived within budget. | `unit_movement` (generated, 173 unit types, 346 checks, 345 pass) | Resolved (terrain, not the unit): on the flat arena `armsptk` peaks at exactly 47.0 and all 173 types pass 346/346, so the overshoot came from slopes on the old map lanes |
| 2026-09-24 | Engine determinism | Seeded `sync_repro` (3000 frames): the upstream baseline reproduces itself bit-identically, and so does our dev build (no nondeterminism from the parallel sim patches). Dev differs from upstream from battle frame 4. Expected in part: merged community PRs change behaviour on purpose (#3354 command pickup, #3240 heading). | `sync_repro --spectate --seed 1234`, base vs dev x2 | Resolved: dev and dev-without-our-sim-patches are bit-identical over all 3000 frames (digest N8D/KBXzp06kQv33QfRUKg==), so our simulation performance patches are behaviour-neutral on this battle; the divergence from upstream comes from the merged community PRs |
| 2026-09-24 | Engine (upstream) | `CGlobalRendering::CalcGLDeltaTime` busy-waits on `GL_QUERY_RESULT_AVAILABLE`; if the query was never issued it spins forever. The upstream-based workbench baseline hung in pregame this way. | `sync_repro` on the baseline engine (hang stack symbolized) | Fixed on our branch (non-blocking query); upstream still has it: worth a PR |
| 2026-09-24 | Workbench | A hung main thread can't run the workbench watchdog; the runner only noticed at its hard timeout. | Same hang | Fixed: the engine's hang detector ends workbench runs with exit 2 |
| 2026-09-24 | BAR balance | `corstorm` (range 475) did no damage to a stationary structure at 428 (90% of range) in 12 s. Its missile has `weaponvelocity` 190 and `weapontimer` 2 s, about 380 elmos of guided flight, so it likely loses guidance before reaching targets near its stated range. | `weapon_range` scenario | **Open**: needs a BAR balance decision |
| 2026-09-24 | Engine memory | The engine sat at ~8.1 GB working set during `weapon_range` (stable over 30 s, so not a runaway leak). | Observed during a run | Instrumented: every window now records memory start, peak, end and growth |
| 2026-09-24 | Engine (our perf patch) | Use-after-free crash in `ILosType::UpdateHeightMapSynced` (`LosHandler.cpp:683`). The LOS-cache "lazy deletion" optimisation left stale pointers in `losCache`; a reactivated-then-recached instance appeared twice, the first copy was deleted, and the next heightmap change (a building flattening ground) dereferenced the second. | `weapon_range` scenario (engine crash, symbolized with `tools/workbench/symbolize.sh`) | Fixed: reverted (9aad7dca59) |
| 2026-09-23 | Workbench | A crash dialog blocked the engine until the runner's hard timeout. | First crashing run | Fixed: no dialogs in workbench mode, crash recorded against the scenario |
| 2026-09-23 | Workbench | A scenario that ended the game (destroyed all commanders) made the run exit 0 with no results. | First `weapon_range` run | Fixed: engine exit before `FinishRun` is exit code 2 |
| 2026-09-23 | BAR + engine | `gui_gameinfo`, `gui_keybind_info`, `gui_teamstats`, `widget_selector` failed to load: in raw-first VFS mode the engine's `LuaUI/Headers/keysym.h.lua` (which uses `setmetatable` since RecoilEngine #3105) shadowed the game's copy inside an empty include env. | Infolog scan in the first real run | Fixed in BAR (load via `VFS.ZIP`); the runner now flags widget load failures on every run |
| 2026-09-23 | Workbench | `--config` profiles were overwritten by the engine after each run. | First run | Fixed: each cell gets its own copy |
